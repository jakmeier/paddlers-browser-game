query LeaderboardQuery($offset: Int!, $limit: Int) {
    scoreboard(offset: $offset, limit: $limit) {
        playersCount,
        playersByKarma {
            displayName,
            karma,
        }
    }
}