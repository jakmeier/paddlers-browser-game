query WorkerTasksQuery($workerId: Int!){
    worker(workerId: $workerId) {
        id
        x
        y
        mana
        tasks {
            id
            x
            y
            taskType
            startTime
            hoboTarget
        }
    }
}