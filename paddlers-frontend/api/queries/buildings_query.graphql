query BuildingsQuery($village_id: Int!) {
  village(villageId: $village_id) {
    id
    buildings {
      id
      x
      y
      level
      buildingType
      buildingRange
      attackPower
      attacksPerCycle
      creation
    }
  }
}