query BuildingsQuery($village_id: Int!) {
  village(villageId: $village_id) {
    buildings {
      id
      x
      y
      buildingType
      buildingRange
      attackPower
      attacksPerCycle
      creation
    }
  }
}