query AttacksQuery($min_attack_id: Int) {
  village(villageId: 1) {
    attacks(minId: $min_attack_id) {
      id
      units {
        id
        color
        hp
        speed
        effects {
          attribute
          startTime
          strength
        }
      }
      departure
      arrival
    }
  }
}